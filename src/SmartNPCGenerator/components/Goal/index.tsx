import { DataField,GeneratorSection } from '../shared';import type { GeneratorCardProps } from '../types';
export function Goal(p:GeneratorCardProps){const x=p.npc?.goal;return <GeneratorSection section="goal" icon="target" title="Goal" description="A current objective emerging from their lived history." locked={p.locked} ready={Boolean(x)} onLock={p.onLock} onRegenerate={()=>p.onRegenerate()}><div className="field-grid"><DataField label="Current Goal" value={x?.currentGoal} onReroll={x?()=>p.onRegenerate('currentGoal'):undefined} wide/><DataField label="Motivation" value={x?.motivation} wide/><DataField label="Obstacle" value={x?.obstacle} wide/></div></GeneratorSection>;}
