import { DataField,GeneratorSection } from '../shared';import type { GeneratorCardProps } from '../types';import { words } from '../../ui/format';
export function AdventureHooks(p:GeneratorCardProps){const hooks=p.npc?.adventureHooks;return <GeneratorSection section="hooks" icon="map" title="Adventure Hooks" description="Three story openings connected to this NPC’s life." locked={p.locked} ready={Boolean(hooks)} onLock={p.onLock} onRegenerate={()=>p.onRegenerate()}><div className="hook-list">{hooks?.map((hook,index)=><DataField key={`${hook.title}-${index}`} label={`${hook.title} · ${words(hook.difficulty)}`} value={<>{hook.summary}<small>Reward: {hook.reward}</small></>} onReroll={()=>p.onRegenerate('hook',index)} wide/>)}</div></GeneratorSection>;}
