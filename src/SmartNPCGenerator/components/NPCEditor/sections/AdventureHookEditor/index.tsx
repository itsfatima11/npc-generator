import { HookDifficulty } from '../../../../types';import type { EditorSectionProps } from '../../types';import { EditorField,EditorSectionCard,EditorSelect,RegenerateButton } from '../../shared';
export function AdventureHookEditor({draft,change,regenerate,errors}:EditorSectionProps){return <EditorSectionCard title="Adventure Hooks" description="Three campaign-ready situations tied to this NPC." errors={errors}>{draft.npc.adventureHooks.map((hook,index)=><article className="editor-hook" key={index}><h4>Hook {index+1}</h4><EditorField label="Title" value={hook.title} onCommit={value=>change(`npc.adventureHooks.${index}.title`,value)}/><EditorField label="Summary" value={hook.summary} multiline rich onCommit={value=>change(`npc.adventureHooks.${index}.summary`,value)}/><EditorSelect label="Difficulty" value={hook.difficulty} options={Object.values(HookDifficulty)} onCommit={value=>change(`npc.adventureHooks.${index}.difficulty`,value)}/><EditorField label="Reward" value={hook.reward} multiline onCommit={value=>change(`npc.adventureHooks.${index}.reward`,value)}/><RegenerateButton onClick={()=>regenerate?.({section:'adventureHooks',index})} label="Preview regenerated hook"/></article>)}</EditorSectionCard>}
