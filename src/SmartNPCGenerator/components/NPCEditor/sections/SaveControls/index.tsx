export function SaveControls({autosave,onAutosave,onSave,onCancel,disabled,saving,confirmation}:{readonly autosave:boolean;readonly onAutosave:(value:boolean)=>void;readonly onSave:()=>void;readonly onCancel:()=>void;readonly disabled:boolean;readonly saving:boolean;readonly confirmation:string}){return <div className="editor-save-controls"><label><input type="checkbox" checked={autosave} onChange={event=>onAutosave(event.target.checked)}/> Auto save</label><span role="status">{confirmation}</span><button type="button" onClick={onCancel}>Discard Draft</button><button type="button" className="button-primary" disabled={disabled||saving} onClick={onSave}>{saving?'Saving…':'Save New Version'}</button></div>}
