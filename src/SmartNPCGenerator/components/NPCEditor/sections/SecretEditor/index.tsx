import { RiskLevel } from '../../../../types';import type { EditorSectionProps } from '../../types';import { EditorField,EditorSectionCard,EditorSelect,StringListEditor } from '../../shared';
export function SecretEditor({draft,change,errors}:EditorSectionProps){const secret=draft.npc.secret;return <EditorSectionCard title="Secret" description="Hidden truth, exposure risk, and everyone currently aware of it." errors={errors}><EditorField label="Secret" value={secret.description} multiline rich onCommit={value=>change('npc.secret.description',value)}/><EditorSelect label="Risk level" value={secret.riskLevel} options={Object.values(RiskLevel)} onCommit={value=>change('npc.secret.riskLevel',value)}/><StringListEditor label="Who knows" values={secret.whoKnows.map(person=>person.name)} onCommit={names=>change('npc.secret.whoKnows',names.map(name=>({name})))}/></EditorSectionCard>}
