import type { EditorSectionProps } from '../../types';import { EditorField,EditorSectionCard,RegenerateButton } from '../../shared';
export function GoalEditor({draft,change,regenerate,errors}:EditorSectionProps){const goal=draft.npc.goal;return <EditorSectionCard title="Goal" description="Current objective, emotional motivation, and primary obstacle." errors={errors}><EditorField label="Current goal" value={goal.currentGoal} multiline rich onCommit={value=>change('npc.goal.currentGoal',value)}/><EditorField label="Motivation" value={goal.motivation} multiline rich onCommit={value=>change('npc.goal.motivation',value)}/><EditorField label="Obstacle" value={goal.obstacle} multiline rich onCommit={value=>change('npc.goal.obstacle',value)}/><RegenerateButton onClick={()=>regenerate?.({section:'goal'})} label="Preview regenerated goal"/></EditorSectionCard>}
