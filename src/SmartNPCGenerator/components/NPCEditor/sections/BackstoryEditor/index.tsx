import type { EditorSectionProps } from '../../types';import { EditorField,EditorSectionCard,RegenerateButton } from '../../shared';
export function BackstoryEditor({draft,change,regenerate,errors}:EditorSectionProps){return <EditorSectionCard title="Backstory" description="The ten-stage life history. Each regeneration proposal affects only its selected chapter." errors={errors}>{draft.npc.backstory.map((chapter,index)=><details className="editor-chapter" key={`${chapter.title}-${index}`} open={index===0}><summary>Chapter {index+1}: {chapter.title}</summary><div><EditorField label="Title" value={chapter.title} onCommit={value=>change(`npc.backstory.${index}.title`,value)}/>{(['beginning','development','climax','ending'] as const).map(stage=><EditorField key={stage} label={stage} value={chapter[stage]} multiline rich onCommit={value=>change(`npc.backstory.${index}.${stage}`,value)}/>)}<EditorField label="Selected result" value={chapter.selectedResult??''} multiline rich onCommit={value=>change(`npc.backstory.${index}.selectedResult`,value||null)}/><RegenerateButton onClick={()=>regenerate?.({section:'backstory',index})} label="Preview regenerated chapter"/></div></details>)}</EditorSectionCard>}
