import type { EditorSectionProps } from '../../types';import { EditorField,EditorSectionCard,RegenerateButton,StringListEditor } from '../../shared';
export function PersonalityEditor({draft,change,regenerate,errors}:EditorSectionProps){const p=draft.npc.personality;return <EditorSectionCard title="Personality" description="Traits, convictions, habits, emotions, desires, and moral boundaries." errors={errors}><StringListEditor label="Traits" values={p.traits} onCommit={value=>{if(value.length===3)change('npc.personality.traits',value);}}/>{(['ideals','bonds','flaws','habits','likes','dislikes','fears'] as const).map(key=><StringListEditor key={key} label={key} values={p[key]} onCommit={value=>change(`npc.personality.${key}`,value)}/>)}{(['secret','greatestStrength','greatestWeakness','biggestRegret','biggestDesire','moralCode'] as const).map(key=><EditorField key={key} label={key.replace(/([A-Z])/g,' $1')} value={p[key]} multiline={key==='moralCode'} onCommit={value=>change(`npc.personality.${key}`,value)}/>)}<RegenerateButton onClick={()=>regenerate?.({section:'personality'})} label="Preview regenerated personality"/></EditorSectionCard>}
