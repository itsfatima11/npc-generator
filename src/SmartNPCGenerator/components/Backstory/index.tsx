import { DataField,GeneratorSection } from '../shared';import type { GeneratorCardProps } from '../types';
export function Backstory(p:GeneratorCardProps){const chapters=p.npc?.backstory;return <GeneratorSection section="backstory" icon="book" title="Backstory" description="Ten connected chapters from childhood to the present." locked={p.locked} ready={Boolean(chapters)} onLock={p.onLock} onRegenerate={()=>p.onRegenerate()}><div className="chapter-list compact">{chapters?.map((chapter,index)=><DataField key={chapter.title} label={`Chapter ${index+1} · ${chapter.title}`} value={chapter.selectedResult} onReroll={()=>p.onRegenerate('chapter',index)} wide/>)}</div></GeneratorSection>;}
