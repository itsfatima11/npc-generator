import { DataField,GeneratorSection } from '../shared';import type { GeneratorCardProps } from '../types';import { words } from '../../ui/format';
export function Secrets(p:GeneratorCardProps){const x=p.npc?.secret;return <GeneratorSection section="secret" icon="lock" title="Secret" description="A hidden truth with real personal consequences." locked={p.locked} ready={Boolean(x)} onLock={p.onLock} onRegenerate={()=>p.onRegenerate()}><div className="field-grid"><DataField label="Hidden Truth" value={x?.description} onReroll={x?()=>p.onRegenerate('description'):undefined} wide/><DataField label="Risk" value={x?words(x.riskLevel):''}/><DataField label="Who Knows" value={x?.whoKnows.map(person=>person.name).join(', ')||'No one else'}/></div></GeneratorSection>;}
