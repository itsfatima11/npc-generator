import { DataField,GeneratorSection } from '../shared';import type { GeneratorCardProps } from '../types';import { words } from '../../ui/format';
export function GeneralInformation(p:GeneratorCardProps){const n=p.npc;return <GeneratorSection section="general" icon="user" title="General Information" description="Identity, ancestry, age, and place in the world." locked={p.locked} ready={Boolean(n)} onLock={p.onLock} onRegenerate={()=>p.onRegenerate()}><div className="field-grid"><DataField label="Name" value={n?.name} onReroll={n?()=>p.onRegenerate('name'):undefined}/><DataField label="Race" value={n?`${n.race}${n.subrace?` · ${n.subrace}`:''}`:''} onReroll={n?()=>p.onRegenerate('race'):undefined}/><DataField label="Age" value={n?`${n.age} years`:''} onReroll={n?()=>p.onRegenerate('age'):undefined}/><DataField label="Gender" value={n?words(n.gender):''}/><DataField label="Alignment" value={n?words(n.alignment):''}/><DataField label="Faith" value={n?.religion??'Unaffiliated'}/><DataField label="Residence" value={n?.residence} wide/></div></GeneratorSection>;}
