import { useState } from 'react';
export function MessageInput({disabled,onSend}:{readonly disabled:boolean;readonly onSend:(content:string)=>void}){const [value,setValue]=useState('');const submit=()=>{const content=value.trim();if(!content||disabled)return;onSend(content);setValue('');};return <div className="dialogue-input"><label htmlFor="dialogue-message">Speak to the NPC</label><textarea id="dialogue-message" value={value} disabled={disabled} onChange={event=>setValue(event.target.value)} onKeyDown={event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();submit();}}} placeholder="Ask a question, make an offer, or describe your action…"/><button type="button" disabled={disabled||!value.trim()} onClick={submit}>Send</button></div>}
