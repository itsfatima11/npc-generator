import type { EquipmentSet,Inventory } from '../../types';
export function EquipmentViewer({equipment,inventory}:{readonly equipment:EquipmentSet;readonly inventory:Inventory}){const byId=new Map(inventory.entries.map(entry=>[entry.item.id,entry.item.name])),names=(ids:readonly string[])=>ids.map(id=>byId.get(id)).filter((name):name is string=>Boolean(name));return <section className="item-subpanel"><h3>Equipment</h3><dl><div><dt>Weapons</dt><dd>{names(equipment.weaponIds).join(', ')||'None'}</dd></div><div><dt>Armor</dt><dd>{equipment.armorId?byId.get(equipment.armorId)??'Unavailable':'None'}</dd></div><div><dt>Shield</dt><dd>{equipment.shieldId?byId.get(equipment.shieldId)??'Unavailable':'None'}</dd></div><div><dt>Tools</dt><dd>{names(equipment.toolIds).join(', ')||'None'}</dd></div><div><dt>Clothing</dt><dd>{names(equipment.clothingIds).join(', ')||'None'}</dd></div><div><dt>Accessories</dt><dd>{names(equipment.accessoryIds).join(', ')||'None'}</dd></div></dl></section>}
