import type { NPC } from '../../../../types';
export function ItemGenerator({npcs,selected,onSelected,onGenerate,disabled}:{readonly npcs:readonly NPC[];readonly selected:string;readonly onSelected:(id:string)=>void;readonly onGenerate:()=>void;readonly disabled:boolean}){return <section className="item-generator-controls"><h3>Inventory Source</h3><label>NPC<select value={selected} onChange={event=>onSelected(event.target.value)}><option value="">Select an NPC</option>{npcs.map(npc=><option value={npc.id} key={npc.id}>{npc.name} · {npc.occupation.title} · {npc.wealth}</option>)}</select></label><button className="button-primary" disabled={disabled} onClick={onGenerate}>Generate Inventory</button><small>Occupation, wealth, lifestyle, location, history, and relationships determine every item.</small></section>}
