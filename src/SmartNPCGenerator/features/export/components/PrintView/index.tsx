import type { ExportDocument } from '../../types';
export function PrintView({document,onClose}:{readonly document:ExportDocument;readonly onClose:()=>void}){const print=()=>{const frame=window.document.getElementById('npc-print-frame') as HTMLIFrameElement|null;frame?.contentWindow?.print()};return <div className="print-overlay" role="dialog" aria-modal="true" aria-label="Print NPC"><section><header><h2>Print Preview</h2><div><button onClick={print}>Print</button><button onClick={onClose}>Close</button></div></header><iframe id="npc-print-frame" title="Printable NPC document" srcDoc={document.content}/></section></div>}
