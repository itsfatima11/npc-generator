import type { ExportDocument } from '../../types';
export function PreviewExport({document}:{readonly document:ExportDocument|null}){if(!document)return <section className="export-preview export-preview--empty"><h3>No export preview</h3><p>Choose permissions and prepare an export from the saved NPC data.</p></section>;const html=document.format==='html'||document.format==='pdf';return <section className="export-preview"><header><div><h3>{document.metadata.title}</h3><span>{document.format} · {document.metadata.audience}</span></div><small>{new Date(document.createdAt).toLocaleString()}</small></header>{html?<iframe title="HTML export preview" srcDoc={document.content}/>:<pre>{document.content}</pre>}</section>}
