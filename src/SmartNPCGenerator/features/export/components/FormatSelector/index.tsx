import type { ExportAudience,ExportFormat } from '../../types';
const formats:readonly ExportFormat[]=['json','markdown','pdf','html','dnd-beyond-style','foundry-vtt','roll20','generic-vtt'];
export function FormatSelector({format,onFormat,audience,onAudience}:{readonly format:ExportFormat;readonly onFormat:(value:ExportFormat)=>void;readonly audience:ExportAudience;readonly onAudience:(value:ExportAudience)=>void}){return <div className="export-format-selector"><label>Format<select value={format} onChange={event=>onFormat(event.target.value as ExportFormat)}>{formats.map(value=><option value={value} key={value}>{value.replaceAll('-',' ')}</option>)}</select></label><label>Audience<select value={audience} onChange={event=>onAudience(event.target.value as ExportAudience)}><option value="dm">Dungeon Master — full</option><option value="player">Player — safe</option></select></label></div>}
