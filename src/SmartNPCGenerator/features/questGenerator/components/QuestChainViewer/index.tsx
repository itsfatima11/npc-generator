import type { QuestChain,Quest } from '../../types';
export function QuestChainViewer({chains,quests}:{readonly chains:readonly QuestChain[];readonly quests:readonly Quest[]}){const byId=new Map(quests.map(quest=>[quest.id,quest]));return <section className="quest-history"><h3>Quest Chains</h3>{chains.length===0?<p>No quest chains have been assembled.</p>:chains.map(chain=><article key={chain.id}><header><strong>{chain.title}</strong><span>{chain.status}</span></header><ol>{chain.questIds.map((id,index)=><li className={id===chain.activeQuestId?'active':''} key={id}><span>Chapter {index+1}</span><strong>{byId.get(id)?.title??'Unavailable quest'}</strong></li>)}</ol></article>)}</section>}
