import type { Quest } from '../../types';
export function QuestDetails({quest}:{readonly quest:Quest}){return <div className="quest-details"><section><h4>Objectives</h4><ol>{quest.objectives.map(objective=><li key={objective.id}><strong>{objective.kind}</strong><span>{objective.description}</span></li>)}</ol></section><section><h4>Encounters</h4>{quest.enemies.map(encounter=><article key={encounter.id}><strong>{encounter.title}</strong><span>{encounter.enemyType} · {encounter.environment}</span><p>{encounter.description}</p><small>Approaches: {encounter.resolutionOptions.join(', ')}</small></article>)}</section><section><h4>Consequences</h4>{quest.consequences.map(item=><article key={item.id}><strong>{item.outcome.replaceAll('-',' ')}</strong><p>{item.description}</p></article>)}</section></div>}
