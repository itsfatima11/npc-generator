import type { Quest } from '../../types';import { DifficultyIndicator } from '../DifficultyIndicator';import { QuestDetails } from '../QuestDetails';import { RewardPanel } from '../RewardPanel';
export function QuestPreview({quest}:{readonly quest:Quest|null}){if(!quest)return <section className="quest-preview quest-empty"><div><h3>No quest generated</h3><p>Select a campaign source with enough connected evidence, then generate a quest.</p></div></section>;return <section className="quest-preview"><header><div><span>{quest.type.replaceAll('-',' ')}</span><h3>{quest.title}</h3><p>{quest.description}</p></div><DifficultyIndicator difficulty={quest.difficulty}/></header><dl><div><dt>Location</dt><dd>{quest.location}</dd></div><div><dt>Giver</dt><dd>{quest.giver}</dd></div><div><dt>Status</dt><dd>{quest.status}</dd></div><div><dt>Participants</dt><dd>{quest.participants.length}</dd></div></dl><QuestDetails quest={quest}/><RewardPanel rewards={quest.rewards}/></section>}
