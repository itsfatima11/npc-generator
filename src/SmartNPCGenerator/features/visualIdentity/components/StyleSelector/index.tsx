import { CAMERA_PRESETS,STYLE_PRESETS } from '../../services';import type { CameraAngle,VisualStyle } from '../../types';
export function StyleSelector({style,camera,onStyle,onCamera}:{readonly style:VisualStyle;readonly camera:CameraAngle;readonly onStyle:(value:VisualStyle)=>void;readonly onCamera:(value:CameraAngle)=>void}){return <div className="visual-style-selector"><label>Visual style<select value={style} onChange={event=>onStyle(event.target.value as VisualStyle)}>{Object.keys(STYLE_PRESETS).map(value=><option key={value} value={value}>{value.replace(/-/g,' ')}</option>)}</select></label><label>Camera<select value={camera} onChange={event=>onCamera(event.target.value as CameraAngle)}>{Object.keys(CAMERA_PRESETS).map(value=><option key={value} value={value}>{value.replace(/-/g,' ')}</option>)}</select></label></div>}
