import type { DialogueNPC } from '../../../aiDialogue';
export function CharacterCard({source,imageUrl}:{readonly source:DialogueNPC;readonly imageUrl:string}){const n=source.npc;return <section className="visual-character-card"><h3>Character Card</h3><div>{imageUrl?<img src={imageUrl} alt={`Portrait of ${n.name}`}/>:<div className="card-no-image">No portrait generated</div>}<article><span>{n.race} · {n.alignment.replace(/-/g,' ')}</span><h4>{n.name}</h4><strong>{n.occupation.title}</strong><blockquote>{source.flavor.quote.text}</blockquote><p>{n.appearance.distinguishingFeatures.join(', ')}. {n.personality.traits.join(', ')}.</p><small>Goal: {n.goal.currentGoal}</small></article></div></section>}
