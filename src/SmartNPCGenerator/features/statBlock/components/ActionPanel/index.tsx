import type { NPCStatBlock,StatAction } from '../../types';
function Group({title,actions}:{readonly title:string;readonly actions:readonly StatAction[]}){if(!actions.length)return null;return <section><h3>{title}</h3>{actions.map(action=><article key={action.id}><header><strong>{action.name}</strong><span>{action.kind} · {action.rulesStatus}</span></header><p>{action.description}</p><small>{action.attackBonus!==null?`Attack ${action.attackBonus>=0?'+':''}${action.attackBonus}`:''}{action.damage?` · ${action.damage}`:''}{action.saveDC!==null?` · DC ${action.saveDC}`:''}</small></article>)}</section>}
export function ActionPanel({block}:{readonly block:NPCStatBlock}){return <div className="stat-actions"><Group title="Actions" actions={block.actions}/><Group title="Bonus Actions" actions={block.bonusActions}/><Group title="Reactions" actions={block.reactions}/><Group title="Legendary Actions" actions={block.legendaryActions}/></div>}
