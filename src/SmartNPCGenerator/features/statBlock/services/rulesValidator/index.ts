import type { NPC } from '../../../../types';import type { NPCStatBlock,StatValidationIssue } from '../../types';
export function validateRules(block:NPCStatBlock,npc:NPC):readonly StatValidationIssue[]{const issues:StatValidationIssue[]=[];for(const score of Object.values(block.abilityScores))if(score.score<3||score.score>30)issues.push(error('ability.range','abilityScores',`${score.ability} must be between 3 and 30.`));if(block.armorClass<5||block.armorClass>30)issues.push(error('combat.ac','armorClass','Armor Class is outside the supported 5e range.'));if(block.hitPoints<1)issues.push(error('combat.hp','hitPoints','Hit points must be positive.'));if(block.spellcasting){if(block.spellcasting.spellSaveDC!==8+block.proficiencyBonus+block.abilityScores[block.spellcasting.ability].modifier)issues.push(error('spell.dc','spellcasting','Spell save DC does not match 8 + proficiency + casting modifier.'));if(block.spellcasting.knownSpells.some(spell=>spell.rulesStatus==='homebrew'))issues.push({code:'spell.homebrew',field:'spellcasting',severity:'warning',message:'Homebrew spells must be explicitly labeled.'});}if(block.legendaryActions.length&&!block.notes.some(note=>/legendary importance/i.test(note)))issues.push(error('actions.legendary','legendaryActions','Legendary actions require explicit legendary narrative importance.'));if(block.equipment?.ownerNpcId!==undefined&&block.equipment.ownerNpcId!==npc.id)issues.push(error('equipment.owner','equipment','Attached inventory belongs to another NPC.'));return issues;}
function error(code:string,field:StatValidationIssue['field'],message:string):StatValidationIssue{return{code,field,severity:'error',message};}
