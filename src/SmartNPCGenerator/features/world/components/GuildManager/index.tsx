import type { WorldViewProps } from '../types';
export function GuildManager({world,npcs}:WorldViewProps){const byId=new Map(npcs.map(n=>[n.id,n]));return <section className="world-card"><header><h3>Guilds</h3><span>{world.guilds.length}</span></header>{world.guilds.length===0?<p className="world-empty">No professional guilds registered.</p>:<div className="world-list">{world.guilds.map(guild=><article key={guild.id}><strong>{guild.name}</strong><span>{guild.profession} · {guild.wealth}</span><small>Leader: {guild.leaderNpcId?byId.get(guild.leaderNpcId)?.name??'Unavailable':'Unassigned'} · {guild.reputation}</small></article>)}</div>}</section>}
