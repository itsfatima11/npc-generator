import type { WorldViewProps } from '../types';
export function FactionManager({world,npcs}:WorldViewProps){const byId=new Map(npcs.map(n=>[n.id,n]));return <section className="world-card"><header><h3>Factions</h3><span>{world.factions.length}</span></header>{world.factions.length===0?<p className="world-empty">No factions have been connected to this world.</p>:<div className="world-list">{world.factions.map(faction=><article key={faction.id}><strong>{faction.name}</strong><span>{faction.type.replaceAll('-',' ')} · {faction.alignment.replaceAll('-',' ')}</span><small>Leader: {faction.leaderNpcId?byId.get(faction.leaderNpcId)?.name??'Unavailable':'Unassigned'} · {faction.memberNpcIds.length} members</small><small>{faction.goals.join(' · ')}</small></article>)}</div>}</section>}
