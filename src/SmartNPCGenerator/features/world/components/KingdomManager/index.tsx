import type { WorldViewProps } from '../types';
export function KingdomManager({world,npcs}:WorldViewProps){const people=new Map(npcs.map(n=>[n.id,n.name])),cities=new Map(world.cities.map(c=>[c.id,c.name]));return <section className="world-card"><header><h3>Kingdoms</h3><span>{world.kingdoms.length}</span></header>{world.kingdoms.length===0?<p className="world-empty">No kingdoms defined.</p>:<div className="world-list">{world.kingdoms.map(k=><article key={k.id}><strong>{k.name}</strong><span>{k.government}</span><small>Capital: {k.capitalCityId?cities.get(k.capitalCityId)??'Unavailable':'Unassigned'} · Ruler: {k.rulerNpcId?people.get(k.rulerNpcId)??'Unavailable':'Unassigned'}</small></article>)}</div>}</section>}
