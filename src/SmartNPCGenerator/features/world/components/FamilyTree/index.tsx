import type { WorldViewProps } from '../types';
export function FamilyTree({world,npcs}:WorldViewProps){const byId=new Map(npcs.map(n=>[n.id,n]));return <section className="world-card"><header><h3>Family Tree</h3><span>{world.familyLinks.length} links</span></header>{world.familyLinks.length===0?<p className="world-empty">No family links recorded.</p>:<div className="family-list">{world.familyLinks.map(link=><article key={link.id}><strong>{byId.get(link.sourceNpcId)?.name??'Unavailable'}</strong><span>{link.relation}</span><strong>{byId.get(link.targetNpcId)?.name??'Unavailable'}</strong><small>{link.history}</small></article>)}</div>}</section>}
