import { nicknameFits,personalityTone } from './nicknameRules';import { titleFits } from './titleRules';import type { GeneratedName,NameCulture,NameGenerationContext,NameValidationIssue,NameValidationResult } from './types';
const SHARED_NICKNAMES=['The Brave','Trueheart','Bright Hand','The Steady','Fox','Whisper','Old Bear','Far-Wanderer','Blackblade','One-Eye','Cold Eye','The Grim'];
export const NameValidator={validate(name:GeneratedName,culture:NameCulture,context:NameGenerationContext):NameValidationResult{const issues:NameValidationIssue[]=[];const identity=context.subrace?.id??context.race.id;if(!culture.raceIds.includes(identity)&&!culture.raceIds.includes(context.race.id)&&context.race.id!=='human'&&context.race.id!=='half-elf')issues.push({code:'CULTURE_RACE_MISMATCH',message:'Naming culture does not support this race or subrace.'});const genderPool=name.genderCategory==='male'?culture.maleNames:name.genderCategory==='female'?culture.femaleNames:culture.neutralNames;const allowedFirstNames=culture.rules.allowsVirtueName?[...genderPool,...culture.neutralNames]:genderPool;if(!allowedFirstNames.includes(name.firstName))issues.push({code:'INVALID_GIVEN_NAME',message:'Given name is not present in the resolved cultural pool.'});if(name.surname===name.firstName)issues.push({code:'DUPLICATED_NAME',message:'Given name and surname must differ.'});if(culture.rules.surnameRequired&&!name.surname)issues.push({code:'MISSING_SURNAME',message:'This culture requires a family or clan name.'});if(name.surname&&![...culture.familyNames,...culture.clanNames].includes(name.surname))issues.push({code:'INVALID_SURNAME',message:'Surname does not match the cultural family or clan pool.'});if(name.honorific&&!culture.honorifics.includes(name.honorific))issues.push({code:'INVALID_HONORIFIC',message:'Honorific is not used by the resolved culture.'});if(name.title&&(!culture.titles.includes(name.title)||!titleFits(name.title,context.occupation.id,context.occupation.category,context.yearsExperience)))issues.push({code:'TITLE_OCCUPATION_MISMATCH',message:'Professional title is incompatible with occupation or experience.'});if(name.nickname&&((!culture.nicknames.includes(name.nickname)&&!SHARED_NICKNAMES.includes(name.nickname))||!nicknameFits(name.nickname,personalityTone(context))))issues.push({code:'INVALID_NICKNAME',message:'Nickname is not supported by culture, personality, or reputation rules.'});return {valid:issues.length===0,issues};}} as const;
