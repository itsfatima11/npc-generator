import { Gender,SocialStatus } from '../../../types';import { pick,unit } from '../deterministic';import { TITLE_RULES,titleFits } from './titleRules';import type { NameCulture,NameGenerationContext } from './types';
export interface GeneratedTitle {readonly honorific:string|null;readonly title:string|null}
const NOBLE_STATUSES=new Set<SocialStatus>([SocialStatus.MinorNoble,SocialStatus.Noble,SocialStatus.Royalty,SocialStatus.Aristocracy]);
export const TitleGenerator={generate(context:NameGenerationContext,culture:NameCulture):GeneratedTitle{const noble=NOBLE_STATUSES.has(context.socialStatus);const desired=context.gender===Gender.Female?'Lady':context.gender===Gender.Male?'Sir':'Honored';const honorific=noble&&culture.honorifics.includes(desired)?desired:null;const pool=TITLE_RULES.filter(rule=>culture.titles.includes(rule.title)&&titleFits(rule.title,context.occupation.id,context.occupation.category,context.yearsExperience)).map(rule=>rule.title);const qualified=context.occupation.socialRespect!=='low'&&context.occupation.socialRespect!=='reviled';const title=qualified&&pool.length>0&&unit(context.seed,'name:title:enabled',context.attempt)>.25?pick(pool,context.seed,'name:title',context.attempt):null;return {honorific,title};}} as const;
