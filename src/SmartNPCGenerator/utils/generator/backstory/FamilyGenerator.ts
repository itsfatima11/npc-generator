import { generateChapter } from './generateChapter';import { appendChapter } from './state';import type { StoryContext,StoryState } from './types';
export const FamilyGenerator={generate(context:StoryContext,state:StoryState):StoryState{const chapter=generateChapter('family',context,state);const familyImpact=context.relationships.parents.length===0?'The early loss or absence of parents created a lasting fear of abandonment':context.relationships.siblings.length>0?'Shared duties with siblings made loyalty practical and demanding':'A small household concentrated every expectation on them';return appendChapter(state,chapter,{familyImpact});}} as const;
