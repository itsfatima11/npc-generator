import { generateChapter } from './generateChapter';import { appendChapter } from './state';import type { StoryContext,StoryState } from './types';
export const AchievementGenerator={generate(context:StoryContext,state:StoryState):StoryState{const achievement=`A major ${context.occupation.category} success established a ${context.occupation.socialRespect} reputation and ${context.wealth.replaceAll('-',' ')} resources`;const prepared={...state,facts:{...state.facts,achievement}};const chapter=generateChapter('greatest-achievement',context,prepared);return appendChapter(prepared,chapter,{achievement});}} as const;
