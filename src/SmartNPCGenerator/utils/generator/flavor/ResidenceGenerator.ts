import { BUILDING_BY_STATUS } from './catalog';import { relationshipName,words } from './context';import type { FlavorContext,ResidenceProfile } from './types';
export const ResidenceGenerator={generate(c:FlavorContext):ResidenceProfile{const n=c.npc;const buildingType=BUILDING_BY_STATUS[n.socialStatus]??(c.isWealthy?'comfortable private townhouse':c.isPoor?'shared rented quarters':'modest private dwelling');const condition=c.isWealthy?'well maintained, deliberately furnished, and visibly secure':c.isPoor?'worn but carefully repaired with limited means':'serviceable, lived-in, and maintained around work demands';const workObject=`materials from ${n.occupation.workplace}`;return {location:n.residence,buildingType,condition,interior:`The interior reflects a ${words(n.socialStatus)} ${n.occupation.title}: ${n.appearance.favoriteColors.join(' and ')||'muted practical colors'}, a dedicated work area, and personal space shaped by ${n.personality.likes[0]??'quiet routines'}.`,importantObjects:[workObject,`a reminder of ${n.goal.currentGoal}`,n.religion?`a private shrine to ${n.religion}`:'a keepsake from their upbringing'],security:c.isWealthy?'stout locks, controlled access, and discreet paid vigilance':c.occupationCategory==='crime'?'concealed entry points, warning threads, and an escape route':c.isPoor?'a reinforced latch and neighbors who watch the passage':'sound locks and a practical alarm known to the occupant',neighbors:`The nearest regular contact is ${relationshipName(n)}; other neighbors know the NPC by their ${n.reputation.toLowerCase()} reputation.`};}} as const;
