import type { DailyRoutine,FlavorContext } from './types';
export const DailyRoutineGenerator={generate(c:FlavorContext):DailyRoutine{const n=c.npc;const devotion=n.religion?`offers a brief observance to ${n.religion}, then `:'';const age=n.age>70?'at a sustainable pace ':'';return {morning:`${n.name} wakes ${age}${devotion}reviews the day’s needs and prepares the tools of a ${n.occupation.title}.`,afternoon:`Works at ${n.occupation.workplace}, following this established routine: ${n.occupation.dailyRoutine}`,evening:`Returns to ${n.residence}, tends important relationships, and takes one concrete step toward ${n.goal.currentGoal}.`,night:`Secures the residence, records anything that could threaten the current goal, and keeps ${n.secret.riskLevel} risks connected to the secret out of sight before resting.`};}} as const;
