import { pick,pickMany } from '../deterministic';import { SPEAKING_STYLES } from './catalog';import { words } from './context';import type { FlavorContext,VoicePitch,VoiceProfile,VoiceSpeed,VocabularyLevel } from './types';
const laughs=['a brief breathy chuckle','a warm, open laugh','a restrained half-laugh','a rough bark of amusement','a quiet smile more heard than seen'];
export const VoiceGenerator={generate(c:FlavorContext):VoiceProfile{const age=c.npc.age;const pitch:VoicePitch=c.npc.voice.toLowerCase().includes('deep')?'low':pick<VoicePitch>(['low','medium','high'],c.seed,'flavor.voice.pitch',c.attempt);const speed:VoiceSpeed=c.isLawful?'measured':pick<VoiceSpeed>(['slow','moderate','quick'],c.seed,'flavor.voice.speed',c.attempt);const vocabularyLevel:VocabularyLevel=/formal|academy|scholar/i.test(`${c.npc.education} ${c.npc.occupation.title}`)?'scholarly':c.isWealthy?'courtly':c.occupationCategory==='crafting'||c.occupationCategory==='nature'?'practical':'educated';const speakingStyle=pick(SPEAKING_STYLES[c.occupationCategory]??['clear and personally distinctive'],c.seed,'flavor.voice.style',c.attempt);const emotionalTone=c.isGood?'earnest but guarded':c.isEvil?'controlled and self-serving':'reserved and pragmatic';const speechHabits=pickMany([`uses ${c.npc.occupation.title.toLowerCase()} metaphors`,`returns conversations to ${c.npc.goal.currentGoal.toLowerCase()}`,c.isReligious?`invokes ${c.npc.religion} in serious moments`:'avoids making promises in absolute terms',age>60?'pauses to recall older events precisely':'speaks from recent experience','lowers their voice when discussing personal risks'],2,c.seed,'flavor.voice.habits',c.attempt);const laughStyle=pick(laughs,c.seed,'flavor.voice.laugh',c.attempt);return {pitch,speed,accent:c.npc.accent,speakingStyle,vocabularyLevel,laughStyle,emotionalTone,speechHabits:[...speechHabits],summary:`${words(pitch)}-pitched, ${speed} voice with ${c.npc.accent}. Speaks in a ${speakingStyle} manner with ${words(vocabularyLevel)} vocabulary.`};}} as const;
