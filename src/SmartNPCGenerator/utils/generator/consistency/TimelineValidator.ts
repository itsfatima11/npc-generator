import { allText,ancestry,problem } from './shared';import type { SectionValidator } from './types';
function meaningful(value:string):Set<string>{return new Set(value.toLowerCase().match(/[a-z]{5,}/g)??[]);}
export const TimelineValidator:SectionValidator=({npc})=>{const issues=[];if(npc.backstory.length!==10)return [problem('backstory','storyQuality','CHAPTER_COUNT','Backstory must contain exactly ten chapters.',20,'critical')];const selected=npc.backstory.map(chapter=>chapter.selectedResult?.trim()??'');if(selected.some(value=>!value))issues.push(problem('backstory','storyQuality','INCOMPLETE_CHAPTER','Every chapter requires an assembled result.',12));if(new Set(selected.map(value=>value.toLowerCase())).size!==10)issues.push(problem('backstory','storyQuality','DUPLICATE_EVENTS','Backstory repeats a major event.',12));const expected=['Birth & Childhood','Family','Education','First Major Event','Career','Relationships','Greatest Conflict','Greatest Achievement','Recent Events','Current Situation'];if(npc.backstory.some((chapter,index)=>chapter.title!==expected[index]))issues.push(problem('backstory','storyQuality','CHRONOLOGY_ORDER','Backstory chapters are not in chronological order.',12));const adult=ancestry(npc)?.adultAge??18;if(npc.age<adult+2&&/retired|decades|lifetime career/i.test(allText(selected)))issues.push(problem('backstory','internalConsistency','IMPOSSIBLE_TIMELINE','Backstory contains events impossible for the NPC age.',15));for(let index=1;index<selected.length;index+=1){const prior=meaningful(selected[index-1]),next=meaningful(selected[index]);if(prior.size>0&&![...prior].some(word=>next.has(word)))issues.push(problem('backstory','storyQuality',`CHAPTER_${index+1}_JUMP`,'Adjacent chapters lack a narrative connection.',2,'warning'));}return issues;};
