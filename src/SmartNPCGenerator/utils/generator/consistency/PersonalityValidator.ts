import { PERSONALITY_TRAITS } from '../../../constants';import { Alignment } from '../../../types';import { allText,problem } from './shared';import type { SectionValidator } from './types';
export const PersonalityValidator:SectionValidator=({npc,flavor})=>{const issues=[];if(npc.personality.traits.length!==3||new Set(npc.personality.traits).size!==3)issues.push(problem('personality','personalityConsistency','TRAIT_COUNT','Personality requires exactly three unique traits.',12));const tags=npc.personality.traits.map(value=>PERSONALITY_TRAITS.find(item=>item.label===value)?.tag);const good=[Alignment.LawfulGood,Alignment.NeutralGood,Alignment.ChaoticGood].includes(npc.alignment),evil=[Alignment.LawfulEvil,Alignment.NeutralEvil,Alignment.ChaoticEvil].includes(npc.alignment);if(good&&tags.filter(value=>value==='negative').length>1)issues.push(problem('personality','personalityConsistency','GOOD_TRAIT_CONFLICT','Good alignment conflicts with predominantly harmful traits.',10));if(evil&&tags.filter(value=>value==='positive').length>1)issues.push(problem('personality','personalityConsistency','EVIL_TRAIT_CONFLICT','Evil alignment conflicts with predominantly selfless traits.',10));const context=allText([npc.goal.currentGoal,npc.secret.description,flavor.quote.text,flavor.voice.summary,...flavor.mannerisms]);if(!npc.personality.traits.some(value=>context.toLowerCase().includes(value.toLowerCase())))issues.push(problem('personality','personalityConsistency','TRAITS_NOT_EXPRESSED','Traits do not influence goal, speech, mannerisms, or secret.',5,'warning'));return issues;};
