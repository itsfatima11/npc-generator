import type { NPC } from '../types';
export function words(value:string):string{return value.replaceAll('-',' ').replace(/\b\w/g,letter=>letter.toUpperCase());}
export function npcMarkdown(npc:NPC):string{return `# ${npc.name}\n\n**${npc.race}${npc.subrace?` (${npc.subrace})`:''} · ${npc.age} years · ${words(npc.alignment)}**\n\n## Occupation\n${npc.occupation.title} — ${npc.occupation.workReputation}\n\n## Appearance\n${npc.appearance.distinguishingFeatures.join(', ')}. ${npc.appearance.clothingStyle}.\n\n## Personality\n${npc.personality.traits.join(', ')}.\n\n## Goal\n${npc.goal.currentGoal}\n\n## Secret\n${npc.secret.description}\n\n## Backstory\n${npc.backstory.map(section=>`### ${section.title}\n${section.selectedResult??''}`).join('\n\n')}\n\n## Adventure Hooks\n${npc.adventureHooks.map(hook=>`- **${hook.title}:** ${hook.summary}`).join('\n')}`;}
